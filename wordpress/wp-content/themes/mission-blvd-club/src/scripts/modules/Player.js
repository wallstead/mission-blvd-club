export default class Player {
    constructor() {
        // var player = $('.radioplayer').radiocoPlayer();

        // player.playToggle();

        // player.event('audioLoaded', function(){
        //     $('.status span').html('Loaded');
        // })

        // player.event('audioPlay', function(e){
        //     $('.status span').html('Playing at '+player.getTime()+' seconds<br />artwork: <a href="'+player.getArtwork(500,500,75)+'" target="_blank">'+player.getArtwork(500,500,75)+'</a>');
        // })

        // player.event('audioPause', function(e){
        //     $('.status span').html('Paused at '+player.getTime() + ' seconds');
        // })

        // player.event('timeUpdate', function(e){
        //     $('.seconds').html(e.newTime);
        // });
    }
  }